let msg="Hello world!!!";

import _ from "lodash";

// Перетин массивов
_.intersection([2, 1], [2, 3]); // => [2]

// capitalize робить букву заголовною
_.capitalize("hello"); // Hello
console.log(_.last(['one', 'two']));